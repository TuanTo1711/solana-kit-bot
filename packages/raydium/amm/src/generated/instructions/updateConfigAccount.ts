/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit'
import { RAYDIUM_AMM_PROGRAM_ADDRESS } from '../programs'
import { getAccountMetaFactory, type ResolvedAccount } from '../shared'

export const UPDATE_CONFIG_ACCOUNT_DISCRIMINATOR = new Uint8Array([240, 32, 10, 152, 8, 45, 87, 58])

export function getUpdateConfigAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_CONFIG_ACCOUNT_DISCRIMINATOR)
}

export type UpdateConfigAccountInstruction<
  TProgram extends string = typeof RAYDIUM_AMM_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountAmmConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountAmmConfig extends string ? WritableAccount<TAccountAmmConfig> : TAccountAmmConfig,
      ...TRemainingAccounts,
    ]
  >

export type UpdateConfigAccountInstructionData = {
  discriminator: ReadonlyUint8Array
  param: number
  owner: Address
}

export type UpdateConfigAccountInstructionDataArgs = {
  param: number
  owner: Address
}

export function getUpdateConfigAccountInstructionDataEncoder(): FixedSizeEncoder<UpdateConfigAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['param', getU8Encoder()],
      ['owner', getAddressEncoder()],
    ]),
    value => ({
      ...value,
      discriminator: UPDATE_CONFIG_ACCOUNT_DISCRIMINATOR,
    })
  )
}

export function getUpdateConfigAccountInstructionDataDecoder(): FixedSizeDecoder<UpdateConfigAccountInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['param', getU8Decoder()],
    ['owner', getAddressDecoder()],
  ])
}

export function getUpdateConfigAccountInstructionDataCodec(): FixedSizeCodec<
  UpdateConfigAccountInstructionDataArgs,
  UpdateConfigAccountInstructionData
> {
  return combineCodec(
    getUpdateConfigAccountInstructionDataEncoder(),
    getUpdateConfigAccountInstructionDataDecoder()
  )
}

export type UpdateConfigAccountInput<
  TAccountAdmin extends string = string,
  TAccountAmmConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>
  ammConfig: Address<TAccountAmmConfig>
  param: UpdateConfigAccountInstructionDataArgs['param']
  owner: UpdateConfigAccountInstructionDataArgs['owner']
}

export function getUpdateConfigAccountInstruction<
  TAccountAdmin extends string,
  TAccountAmmConfig extends string,
  TProgramAddress extends Address = typeof RAYDIUM_AMM_PROGRAM_ADDRESS,
>(
  input: UpdateConfigAccountInput<TAccountAdmin, TAccountAmmConfig>,
  config?: { programAddress?: TProgramAddress }
): UpdateConfigAccountInstruction<TProgramAddress, TAccountAdmin, TAccountAmmConfig> {
  // Program address.
  const programAddress = config?.programAddress ?? RAYDIUM_AMM_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    ammConfig: { value: input.ammConfig ?? null, isWritable: true },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  const instruction = {
    accounts: [getAccountMeta(accounts.admin), getAccountMeta(accounts.ammConfig)],
    programAddress,
    data: getUpdateConfigAccountInstructionDataEncoder().encode(
      args as UpdateConfigAccountInstructionDataArgs
    ),
  } as UpdateConfigAccountInstruction<TProgramAddress, TAccountAdmin, TAccountAmmConfig>

  return instruction
}

export type ParsedUpdateConfigAccountInstruction<
  TProgram extends string = typeof RAYDIUM_AMM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    admin: TAccountMetas[0]
    ammConfig: TAccountMetas[1]
  }
  data: UpdateConfigAccountInstructionData
}

export function parseUpdateConfigAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateConfigAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts')
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      ammConfig: getNextAccount(),
    },
    data: getUpdateConfigAccountInstructionDataDecoder().decode(instruction.data),
  }
}
