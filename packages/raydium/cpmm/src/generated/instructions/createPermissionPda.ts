/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { RAYDIUM_CP_SWAP_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CREATE_PERMISSION_PDA_DISCRIMINATOR = new Uint8Array([
  135, 136, 2, 216, 137, 169, 181, 202,
]);

export function getCreatePermissionPdaDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_PERMISSION_PDA_DISCRIMINATOR
  );
}

export type CreatePermissionPdaInstruction<
  TProgram extends string = typeof RAYDIUM_CP_SWAP_PROGRAM_ADDRESS,
  TAccountOwner extends
    | string
    | AccountMeta<string> = 'GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ',
  TAccountPermissionAuthority extends string | AccountMeta<string> = string,
  TAccountPermission extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountPermissionAuthority extends string
        ? ReadonlyAccount<TAccountPermissionAuthority>
        : TAccountPermissionAuthority,
      TAccountPermission extends string
        ? WritableAccount<TAccountPermission>
        : TAccountPermission,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreatePermissionPdaInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CreatePermissionPdaInstructionDataArgs = {};

export function getCreatePermissionPdaInstructionDataEncoder(): FixedSizeEncoder<CreatePermissionPdaInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CREATE_PERMISSION_PDA_DISCRIMINATOR,
    })
  );
}

export function getCreatePermissionPdaInstructionDataDecoder(): FixedSizeDecoder<CreatePermissionPdaInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCreatePermissionPdaInstructionDataCodec(): FixedSizeCodec<
  CreatePermissionPdaInstructionDataArgs,
  CreatePermissionPdaInstructionData
> {
  return combineCodec(
    getCreatePermissionPdaInstructionDataEncoder(),
    getCreatePermissionPdaInstructionDataDecoder()
  );
}

export type CreatePermissionPdaAsyncInput<
  TAccountOwner extends string = string,
  TAccountPermissionAuthority extends string = string,
  TAccountPermission extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner?: TransactionSigner<TAccountOwner>;
  permissionAuthority: Address<TAccountPermissionAuthority>;
  /** Initialize config state account to store protocol owner address and fee rates. */
  permission?: Address<TAccountPermission>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCreatePermissionPdaInstructionAsync<
  TAccountOwner extends string,
  TAccountPermissionAuthority extends string,
  TAccountPermission extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAYDIUM_CP_SWAP_PROGRAM_ADDRESS,
>(
  input: CreatePermissionPdaAsyncInput<
    TAccountOwner,
    TAccountPermissionAuthority,
    TAccountPermission,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreatePermissionPdaInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountPermissionAuthority,
    TAccountPermission,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RAYDIUM_CP_SWAP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    permissionAuthority: {
      value: input.permissionAuthority ?? null,
      isWritable: false,
    },
    permission: { value: input.permission ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.owner.value) {
    accounts.owner.value =
      'GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ' as Address<'GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ'>;
  }
  if (!accounts.permission.value) {
    accounts.permission.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 101, 114, 109, 105, 115, 115, 105, 111, 110])
        ),
        getAddressEncoder().encode(
          expectAddress(accounts.permissionAuthority.value)
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.permissionAuthority),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreatePermissionPdaInstructionDataEncoder().encode({}),
    programAddress,
  } as CreatePermissionPdaInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountPermissionAuthority,
    TAccountPermission,
    TAccountSystemProgram
  >);
}

export type CreatePermissionPdaInput<
  TAccountOwner extends string = string,
  TAccountPermissionAuthority extends string = string,
  TAccountPermission extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner?: TransactionSigner<TAccountOwner>;
  permissionAuthority: Address<TAccountPermissionAuthority>;
  /** Initialize config state account to store protocol owner address and fee rates. */
  permission: Address<TAccountPermission>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCreatePermissionPdaInstruction<
  TAccountOwner extends string,
  TAccountPermissionAuthority extends string,
  TAccountPermission extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAYDIUM_CP_SWAP_PROGRAM_ADDRESS,
>(
  input: CreatePermissionPdaInput<
    TAccountOwner,
    TAccountPermissionAuthority,
    TAccountPermission,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreatePermissionPdaInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountPermissionAuthority,
  TAccountPermission,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RAYDIUM_CP_SWAP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    permissionAuthority: {
      value: input.permissionAuthority ?? null,
      isWritable: false,
    },
    permission: { value: input.permission ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.owner.value) {
    accounts.owner.value =
      'GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ' as Address<'GThUX1Atko4tqhN2NaiTazWSeFWMuiUvfFnyJyUghFMJ'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.permissionAuthority),
      getAccountMeta(accounts.permission),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreatePermissionPdaInstructionDataEncoder().encode({}),
    programAddress,
  } as CreatePermissionPdaInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountPermissionAuthority,
    TAccountPermission,
    TAccountSystemProgram
  >);
}

export type ParsedCreatePermissionPdaInstruction<
  TProgram extends string = typeof RAYDIUM_CP_SWAP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    permissionAuthority: TAccountMetas[1];
    /** Initialize config state account to store protocol owner address and fee rates. */
    permission: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreatePermissionPdaInstructionData;
};

export function parseCreatePermissionPdaInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreatePermissionPdaInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      permissionAuthority: getNextAccount(),
      permission: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreatePermissionPdaInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
